{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "node -e \"const http = require('http'); const s = http.createServer((q,r) => { r.writeHead(200); r.end('ok'); }); s.listen(process.env.PORT || 3000, '0.0.0.0', () => console.log('LISTENING on', process.env.PORT));\"",
    "healthcheckPath": "/",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
